# SmartRewrite Core Principles

## 1. 原始母稿原则

**任何场景转写都必须回到原始母稿，不允许以其他 Renderer 的输出作为输入源。**

原始母稿是当前转写任务指定的完整正式文章，是所有 Renderer 的共同内容来源。

各 Renderer 彼此平级，分别从原始母稿生成自己的场景文稿。一个 Renderer 的输出不能成为另一个 Renderer 的输入。

## 2. Renderer 边界

Core 定义所有 Renderer 共同遵守的内容来源和跨场景边界。

Renderer 只定义具体场景下的转写方式、表达结构和输出形态。
